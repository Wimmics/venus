# Chart title

The `title` property sets a text title displayed above the visualization.

```js
encoding: {
  title: "My Chart Title"
}
```

## Properties

| Property | Type | Description |
|---|---|---|
| `title` | `string` | Title displayed above the chart/graph. Possible values: any non-empty string. <br>**Default:** none (title hidden when unset). |
